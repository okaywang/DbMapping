<Window x:Class="DbMapping.MappingList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MappingList" Height="300" Width="300" Loaded="Window_Loaded">
    <Grid>
        <ListView ItemsSource="{Binding}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding SourceTableName}" Header="源表" />
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="-->" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding TargetTableName}" Header="目标表" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
